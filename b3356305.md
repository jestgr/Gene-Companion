# GeoPro Camp Companion

Run with:

```bash
pip install -r requirements.txt
streamlit run app.py
```

This first attempt is a mobile-friendly Streamlit app with:
- reusable checklists
- gear inventory with storage locations
- campground notes
